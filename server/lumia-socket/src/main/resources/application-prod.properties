# ====================
# 프로덕션 환경 (WebSocket)
# ====================

# JWT 설정 (lumia-api와 동일 - 환경 변수 필수)
app.jwt.secret=${JWT_SECRET}

# 로깅
logging.level.com.lumiaops=WARN
logging.level.org.springframework.messaging=WARN

# WebSocket 허용 Origin
spring.websocket.allowed-origins=${WS_ALLOWED_ORIGINS:https://lumia-ops.com}
